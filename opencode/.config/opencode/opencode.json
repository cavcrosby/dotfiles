{
    "$schema": "https://opencode.ai/config.json",
    "plugin": [
        [
            "@cavcrosby/opencode-rename-export",
            {
                "format": "session-{date}-{time}-{title}.md"
            }
        ]
    ]
}
